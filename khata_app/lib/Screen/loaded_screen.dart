import 'package:flutter/material.dart';

class LoadedScreen extends StatelessWidget {
  const LoadedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:  Center(child: Text('Loaded Screen')),
    );
  }
}
